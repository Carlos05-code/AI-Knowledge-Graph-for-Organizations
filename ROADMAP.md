# Roadmap

Status of the **AI Knowledge Graph for Organizations** platform. Legend: ✅ done ·
🔄 in progress · ⬜ planned.

## Current status

- ✅ Backend API complete (NestJS 11, 16 controllers, ~60 endpoints, WebSocket chat)
- ✅ Databases wired (PostgreSQL 16 + Prisma, Neo4j, Qdrant, OpenSearch BM25 keyword index, Redis cache w/ in-memory fallback)
- ✅ Flutter app shell (login/register, chat w/ citations, hybrid search, graph explorer, profile, admin, documents, connectors, meetings, policies, notifications)
- ✅ CI (GitHub Actions: backend lint/test/e2e/build/docker, frontend analyze/build/docker, compose validation — full pipeline confirmed green live end-to-end 2026-09-18)
- ✅ Docker Compose stack (18 services), Kubernetes manifests; observability stack (Prometheus alerts.yml + provisioned Grafana dashboards + Loki/promtail log shipping + exporters)
- ✅ Tests: 257 unit + 80 e2e + 28 frontend tests, all passing
- ✅ Documentation suite (docs/)

## Milestones

| # | Milestone | Status | Notes |
|---|---|---|---|
| 1 | Repository foundation | ✅ | Monorepo, .github, docs, scripts |
| 2 | Flutter application shell | ✅ | Auth, shell, routing, theme |
| 3 | Backend foundation | ✅ | NestJS modules, global pipe/filters/interceptors |
| 4 | Authentication | ✅ | JWT login/register/refresh/me, RBAC roles |
| 5 | User management | ✅ | Profile edit, org members list + search, role/status management (ADMIN), self-demotion guard; org invitations (invite/accept/revoke + accept-link email) |
| 6 | Connector framework | ✅ | Registry + 14 real adapters — Slack (auth.test, files.list/download, channel export), GitHub (recursive git-tree listing + raw file sync via Octokit), Google Drive (OAuth file sync, Docs/Sheets/Slides exported to text/csv), Gmail (OAuth, recursive MIME-part text extraction), Notion (page search + block-to-markdown), Jira (JQL search + ADF-to-markdown + comments), Linear (GraphQL issue search + comments), Confluence (CQL search + storage-format-to-markdown), GitLab (repo tree + raw file sync, self-hosted `baseUrl` supported), OneDrive + SharePoint (Microsoft Graph, app-only client-credentials OAuth via shared `MicrosoftGraphAuth`), Outlook (Graph mail API, plain-text bodies via `Prefer: outlook.body-content-type="text"`), Teams (Graph channel messages, HTML-to-text stripped), Dropbox (OAuth2 refresh-token or long-lived token, recursive `list_folder`); CRUD/test/sync UI + run history. All originally-planned connectors are now shipped |
| 7 | Document ingestion pipeline | ✅ | Upload → checksum → chunk (512/64) → Qdrant; docs UI (list/upload/detail/process/delete); OCR & versions partial |
| 8 | OCR pipeline | ✅ | Tesseract (`tesseract.js`) for scanned PDFs/images: `OcrService` w/ `isOcrCandidate` + graceful fallback, wired into document processing (OCR'd text → chunked/indexed, `metadata.ocrExtracted`); hardened: PDF text-layer fast path (`pdf-parse`), per-page scanned-PDF OCR (`pdf-to-img`), `OCR_MAX_PAGES` cap, `OCR_MIN_CONFIDENCE` threshold, language packs (`eng+spa`), `metadata.ocrPages/ocrConfidence` |
| 9 | Embedding service | ✅ | OpenAI `text-embedding-3-small` + deterministic fallback |
| 10 | Vector database integration | ✅ | Qdrant, cosine, collection `knowledge_chunks` |
| 11 | Knowledge Graph service | ✅ | Neo4j CRUD, subgraph via APOC, graph explorer UI |
| 12 | Search engine | ✅ | Keyword (OpenSearch BM25, org-scoped, Postgres-ILIKE fallback when OpenSearch is down) + semantic + graph hybrid |
| 13 | Hybrid retrieval | ✅ | Reranked fusion across 3 sources |
| 14 | AI chat | ✅ | REST + WebSocket streaming (OpenAI), RAG context |
| 15 | Citations | ✅ | `sources` in chat + WebSocket responses; citation chips in chat UI |
| 16 | Meeting intelligence | ✅ | CRUD + real LLM-backed summarize endpoint (see milestone 35); meetings UI (create/list/detail/summarize/delete) |
| 17 | Expertise discovery | ✅ | Expertise scores, topic search, org summary |
| 18 | Policy search | ✅ | CRUD + search + document linking; policies UI (filter/search/create/edit/activate/delete) |
| 19 | Notifications | ✅ | Domain-event notifications API + UI (screen, unread badge in shell, mark read/all, swipe delete) |
| 20 | Admin dashboard | ✅ | Metrics + audit logs API; dashboard UI (Overview/Members/Audit Logs tabs) |
| 21 | Monitoring | ✅ | Prometheus `/api/v1/metrics`, winston, health checks |
| 22 | Performance optimization | 🔄 | Pagination done; 16 query indexes (migration `add_performance_indexes`, 2026-08-18) + caching on hot paths (notification unread count, admin dashboard stats, expertise summary); query tuning done (audit: no N+1; batch/join queries confirmed); connection pooling via PgBouncer (transaction mode, connection_limit=9, migrations bypass pooler) |
| 23 | Security hardening | 🔄 | Helmet, bcrypt, validation, global rate limiting, graph auth, Swagger prod gate, DB-backed refresh token rotation + logout revocation, `npm audit` CI gate (0 high), connector credentials encrypted at rest (AES-256-GCM), versioned ciphertext (`akg:v{N}:`, `ENCRYPTION_KEYS` chain), DB-backed JWT secret rotation endpoint (`POST /admin/secrets/rotate-jwt`, dual-key grace), VIEWER read-only enforcement (RolesGuard blocks non-GET for VIEWER; WS chat role-gated: multi-secret verify + DB revalidation on connect, writes denied for VIEWER) — see SECURITY_SPEC remaining items |
| 24 | Testing | 🔄 | 257 unit + 80 e2e (split into 14 per-module files under `test/api/`, now run in CI — see "Quarter ahead" below) + 28 frontend tests; k6 scripts (login/search/chat) + JWT security edge cases done; k6 soak added; ZAP baseline wired into CI (weekly + manual, fixed a YAML indentation bug that had silently kept it out of `jobs:`) |
| 25 | Production deployment | done | Compose stack completed (18 services: exporters, promtail, alertmanager, healthchecks, resource limits); K8s hardened (non-root, read-only root FS, PDBs, termination grace, TLS via cert-manager); HTTP metrics interceptor (http_requests_total + http_request_duration_seconds); alerts.yml rule set + Alertmanager webhook routing; AKG Backend Overview Grafana dashboard; frontend nginx on 8080 for non-root (uid 101); staging env defined in DEVOPS_SPEC |

## Known gaps tracked for next releases

- Keycloak SSO - resolved 2026-08-20: POST /auth/sso/keycloak (RS256 verify via JWKS w/ 5-min cache + 30 s exp skew, iss/aud checks, link by keycloakId then email, auto-provisioning w/ realm-role mapping admin/user/viewer to ADMIN/USER/VIEWER, default USER); GET /auth/sso/keycloak/status; enabled when KEYCLOAK_URL + KEYCLOAK_REALM are set; 9 unit + 4 e2e tests added.
- All shell routes are wired to real screens (no "Coming soon" placeholders); typed models (freezed) planned. `freezed`/`json_serializable` were pruned with the other dead deps; a small `json_annotation`-only codegen for models could return later.
- Widget test suite: 10 widget + 15 provider tests (login/register, admin gating + invites, chat citations, notifications UI + provider, router redirect/shell; search/chat/notifications/invitations providers).
- Backend `npm run lint` gate restored — resolved 2026-08-17: `eslint.config.mjs` now exits 0 (0 errors / 1093 warnings). Legacy `no-unsafe-*` rules (assignment/member-access/call/return) and `no-require-imports` downgraded to `warn` (intentional lazy `require()` for optional deps: nodemailer, pdf-parse, pdf-to-img, googleapis, jsonwebtoken); `no-unused-vars` configured with `^_` ignore patterns; `dist/`/`coverage/`/`uploads/` ignored; leftover 64 errors fixed (require-await stubs, enum comparisons, unused imports/args, empty blocks, unbound methods).
- Org-scoped delete enforcement — **resolved 2026-08-17**: `meetings.delete`/`policies.delete` now verify the record belongs to the caller's org (`findFirst({ id, organizationId })` → `404` otherwise), `notifications.markAsRead`/`delete` verify ownership (`findFirst({ id, userId })` → `404`), `meetings.generateSummary`/`policies.update` now throw `NotFoundException` (404) instead of generic 500; chat `deleteConversation` was already ownership-scoped. 8 new e2e cases; 90 unit + 70 e2e green.
- **#26: 17 high-severity `npm audit` findings, all requiring breaking upgrades — deferred, gate lowered to `critical` (2026-09-18)**: surfaced when e2e-in-CI work (milestone 25) finally got the backend job running far enough to reach the audit step for the first time — it had never actually executed before, since the workflow file was invalid until milestone 24. Three root chains: `deepmerge-ts` (pulled in by Prisma's dev CLI tooling; fix needs a Prisma downgrade to `6.12.0`), `multer` (via `@nestjs/platform-express`, the module the `/upload` endpoint runs on; fix needs `@nestjs/platform-express@12.0.3`, i.e. a NestJS 11→12 major bump touching decorators/guards/DI across the whole backend, and likely `@nestjs/platform-socket.io` too since `ChatGateway` uses it), and `pdfjs-dist` (via `pdf-to-img`, used by the OCR pipeline for scanned PDFs; fix needs `pdf-to-img@7.0.0`). None of the three has a non-breaking fix available (`npm audit fix` alone does nothing; `--force` is required and explicitly warns of breaking changes for all three). Asked the user how to handle this rather than force a framework major-version migration as a drive-by fix at the tail end of an unrelated CI-debugging session; they chose to lower `ci.yml`'s gate from `--audit-level=high` to `--audit-level=critical` (confirmed clean at that level: 0 critical, same 17 high) so CI stays green today, with the real fix — a deliberate, separately-reviewed NestJS 12 (+ platform-express, platform-socket.io) upgrade, a Prisma downgrade or alternative, and a `pdf-to-img` bump — tracked here rather than silently dropped. **Not started**: no work has begun on the actual migration.

## Quarter ahead (priority order)

1. ✅ Secrets rotation — done: versioned ciphertext (`akg:v{N}:` + `ENCRYPTION_KEYS`), `AppSecret` table (encrypted at rest), `POST /admin/secrets/rotate-jwt` with dual-key grace (env + DB secrets verified/signed), 67 unit + 46 e2e green.
2. ✅ OCR perf/hardening — done: PDF text-layer fast path (`pdf-parse`), per-page scanned-PDF OCR (`pdf-to-img`, `OCR_MAX_PAGES` cap), `OCR_MIN_CONFIDENCE` threshold, language packs (`OCR_LANGUAGE` comma → `eng+spa`), per-document OCR metadata (`ocrPages`/`ocrConfidence`); 73 unit + 46 e2e green.
3. ✅ Invitation email follow-ups — done: SMTP hardening (`SMTP_REQUIRE_TLS`, `SMTP_REJECT_UNAUTHORIZED`, `SMTP_RETRIES`, `SMTP_RETRY_DELAY_MS` with per-attempt backoff), `POST /invitations/:id/resend` (fresh token + renewed 7-day expiry, PENDING only), `OutboundEmail` audit log (every attempt, best-effort, non-blocking); 76 unit + 49 e2e green.
4. ✅ VIEWER role enforcement — done: `RolesGuard` now denies every non-GET/HEAD/OPTIONS request for VIEWER (`403 VIEWER role is read-only`), attached to the JWT-only write controllers (chat, upload, meetings, notifications, users), role taken from the DB per request; 13 e2e VIEWER cases + `roles.guard.spec` (6) added; 82 unit + 62 e2e green.
5. ✅ WebSocket chat role-gating — done: `ChatGateway` verifies tokens against every active JWT secret (env + rotated `AppSecret` rows via `SecretsService`), revalidates the DB user on connect (`isActive`, real role), and denies `message:send`/`conversation:delete` for VIEWER (reads allowed); `chat.gateway.spec` (8 tests); 90 unit + 62 e2e green.
6. ✅ Lint gate restoration — done: CI `npm run lint` passes again (0 errors; 1093 legacy warnings documented above); `--fix` auto-formats; leftover errors fixed across adapters/services/specs; flaky e2e `memory_heap` health check eliminated by mocking `MemoryHealthIndicator` in `app.e2e-spec.ts`; 90 unit + 62 e2e green (2 consecutive full e2e runs).
7. ✅ Org-scoped delete enforcement — done: tenant/ownership scoping enforced on `meetings.delete`, `policies.delete`, `notifications.markAsRead`, `notifications.delete` (scoped `findFirst` → `404 Not Found` when the record is not in the caller's org / not owned); `generateSummary` + `policies.update` now return 404 instead of generic 500; e2e suites hardened against slow-CI hook timeouts (30 s `beforeAll`); 8 new e2e cases; 90 unit + 70 e2e green.
8. ✅ Performance optimization (part 1) — done: 16 query indexes added (`add_performance_indexes` migration: User/Document/DocumentVersion/Chunk/Connector/ConnectorRun/Conversation/Message/Meeting/MeetingParticipant/Notification/Policy); `CacheService` fail-open wrapper over `CACHE_MANAGER` (Redis w/ in-memory fallback) applied to hottest paths — notification unread count (15 s TTL, invalidated on write), admin dashboard stats (60 s), expertise summary (300 s); chat list/history and doc lists audited (already lean, no N+1); 14 new unit tests (cache.service 5, notifications.service 9 incl. milestone-7 404 behavior); 104 unit + 70 e2e green.
9. ✅ Load/security test suites (part 1) — done: k6 scripts for login (100 VU, P95 < 500 ms), hybrid search (60 VU, P95 < 300 ms), chat streaming (20 concurrent WS) in `backend/test/k6/` with npm `test:load:*` scripts + README (run against staging; ZAP scan + soak tests remain); 6 JWT security edge cases in e2e (expired → 401, tampered signature → 401, deleted user → 401, inactive user → 401, forged role/org claims rejected — role from DB → 403, missing Authorization → 401); 76 e2e green.
10. Production deployment (part 1) - done: HTTP request metrics via global interceptor (http_requests_total + http_request_duration_seconds, exported at /api/v1/metrics); Prometheus lerts.yml (backend down, 5xx > 5%, P95 > 2s, heap > 85%, host CPU/mem, exporter/Redis down, Postgres connections); compose completed with postgres-exporter, node-exporter, promtail (docker-socket discovery -> Loki), healthchecks + resource limits on all 16 services; Grafana provisions AKG Backend Overview dashboard; K8s hardened (non-root uids 1000/101, read-only root FS + emptyDir mounts, capabilities dropped, seccomp RuntimeDefault, PDBs, terminationGracePeriodSeconds); frontend nginx moved to 8080 (non-root). Remaining: Ingress TLS, alertmanager/receiver wiring, dashboard tweaks; 108 unit + 76 e2e green.
11. Production deployment (part 2) - done: Alertmanager bundled and wired (prometheus.yml targets lertmanager:9093, compose service with healthcheck + limits, webhook receiver config - placeholder URL to replace for prod); staging environment defined in DEVOPS_SPEC (full 17-service stack, JSON logs, pinned CORS, swagger off); Ingress TLS confirmed (cert-manager letsencrypt-prod + akg-tls secret, ssl-redirect); compose validation CI job already in place (docker compose config); 108 unit + 76 e2e green.
12. ZAP scan + load soak - done: k6 soak.js (32 min, 20 VU hold, 30% login / 70% search + WS chat smoke, P95 < 1 s / P99 < 2.5 s, error rate < 1%, 
pm run test:load:soak); OWASP ZAP baseline (ackend/test/zap/zap-baseline.sh, Docker passive scan, fails on HIGH, HTML/JSON/MD/XML reports) + CI job zap-baseline (weekly Mon 03:00 + workflow_dispatch, STAGING_URL secret, artifact upload). Remaining: authenticated active scanning, 100-file upload soak, 20k-chunk vector refresh.
13. Performance optimization (part 2) - done: PgBouncer connection pooling (compose service, transaction mode, Prisma connection_limit=9&pool_timeout=10&pgbouncer=true, migrations run directly against postgres); slow-query instrumentation in PrismaService (SLOW_QUERY_MS, default 500 ms, winston/Nest warning with query target); hot-path query audit across search/chat/recommendations/gaps/meetings/policies/notifications/users - no N+1 found, includes/joins and pagination confirmed clean; dead include: { organization: false } removed from admin stats; docs updated (DATABASE_SPEC pooling section, BACKEND_SPEC env, DEVOPS_SPEC compose 18 services, .env.example).
14. ✅ OpenSearch wired in (2026-09-17) — done: OpenSearch was provisioned (compose, k8s, env vars, docs) but had zero application code using it; keyword search ran on Postgres ILIKE only despite the "hybrid BM25 + vector + graph" claim. `OpenSearchService` added (fail-soft connect/index-ensure, matching the Qdrant/Neo4j resilience pattern); `SearchService.hybridSearch` and `ChatService.retrieveContext` now query OpenSearch BM25 first (org-scoped `multi_match` on title/content) and fall back to Postgres ILIKE only when OpenSearch is unavailable or errors; document chunk index/delete now write to OpenSearch alongside Qdrant. Fixed along the way: `docker-compose.yml` had `OPENSEARCH_HOST=http://...` and an unauthenticated healthcheck against an instance with the security plugin enabled (would 401/hang) — now `https://` + basic auth; `ChatService.keywordSearch` had no `organizationId` filter at all (cross-tenant RAG context leak) — now scoped like `SearchService`'s equivalent; a standalone `refresh-vector.ts` backfill script had a syntax error and three API-mismatch bugs that failed `tsc`/`npm run build` outright — fixed. 18 new unit tests (`opensearch.service.spec`, `search.service.spec`, extended `chat.service.spec`); 135 unit tests green. K8s (`backend-deployment.yml` + `secrets.yml`) and `.env.example` updated to match. **Not yet done, closed by milestone 30**: OpenSearch isn't provisioned in CI, so this path is unit-tested via mocks only — not exercised end-to-end until a real cluster is available (e.g. in a docker-compose smoke test or staging).
15. ✅ Notion, Jira, Linear connector adapters (2026-09-17) — done: the Flutter connectors UI already offered NOTION/JIRA/LINEAR as connector types (with credential hints) and the Prisma `ConnectorType` enum + `sourceForType` mapping already covered all 11 non-CUSTOM types, but `ConnectorRegistryService` only had adapters for GOOGLE_DRIVE/SLACK/GITHUB — creating a Notion/Jira/Linear connector and clicking "test" or "sync" threw `No adapter registered for connector type`. Added three real fetch-based adapters (no new SDK dependencies, matching the Slack adapter's style): `NotionAdapter` (search API + `/blocks/{id}/children` pagination, block-to-markdown for headings/lists/quotes/code, depth-capped recursion for nested blocks), `JiraAdapter` (JQL search via `/rest/api/3/search` with HTTP Basic auth from email+API token, ADF-to-markdown for descriptions, comments included), `LinearAdapter` (GraphQL against `api.linear.app/graphql`, raw API-key auth header per Linear's docs, issues + comments). All three registered in `ConnectorRegistryService`; `sourceForType`/persistence/chunking needed no changes since they were already generic. Found and fixed a real pagination bug while writing `JiraAdapter`'s tests: `searchIssues` didn't use the server-reported `total` to know when to stop paginating, so it depended on the page happening to come back empty — switched to `issues.length < total` as the loop condition. 25 new unit tests (8 Notion, 10 Jira, 7 Linear) + 8 for a new `connector-registry.service.spec.ts` (none existed before); 168 unit tests green, clean typecheck/build/lint. README moved Notion/Jira/Linear from Planned to Shipped. **Not yet done**: none of the three has been exercised against a real Notion workspace / Jira site / Linear team — verified via mocked HTTP/GraphQL only, since no live credentials are available in this environment.
16. ✅ Confluence, GitLab connector adapters (2026-09-17) — done: `ConfluenceAdapter` (CQL content search via `/wiki/rest/api/content/search`, same email+API-token Basic auth as Jira since both are Atlassian Cloud products, storage-format-to-markdown converter that strips `ac:*` macro blocks and converts headings/lists/paragraphs) and `GitLabAdapter` (repository tree listing + raw file content via `/api/v4`, `PRIVATE-TOKEN` auth, `baseUrl` override for self-hosted GitLab instances, binary-file skip via null-byte heuristic matching the Slack adapter's approach). Both registered in `ConnectorRegistryService`. Also added CONFLUENCE and GITLAB entries (with credential hints) to the Flutter connectors dropdown, which previously only listed 6 of the 8 now-real types. 18 new unit tests (9 Confluence, 9 GitLab) + registry test extended to cover all 8 types; 188 unit tests green, clean typecheck/build/lint. README/ROADMAP connector counts updated (6 → 8 real adapters). **Known gap surfaced, not yet fixed**: while working on this, found that `GitHubAdapter.syncAll()` is a hardcoded no-op (`return { documentsSynced: 0, errors: [], metadata: {} }`) and `downloadFile()`/`getFileMetadata()` don't actually work either — `authenticate()` and `listFiles()` (root directory only, non-recursive) are the only genuinely functional parts. This means the GitHub connector cannot sync any documents today, despite milestone 6 (and an earlier ROADMAP entry) describing it as providing "repo content sync." Fixing it is a same-shaped problem to what GitLabAdapter just solved (recursive tree + raw content fetch, this time via the already-installed Octokit client) — flagged here rather than fixed silently, since it wasn't part of what was asked for this pass.
17. ✅ GitHub `syncAll` fixed + two missing-dependency bugs found (2026-09-18) — done: rewrote `GitHubAdapter` to actually sync — `listTree()` fetches the repo's default (or configured) branch and walks it recursively via Octokit's `git.getTree({recursive: '1'})`, `syncAll()` downloads each blob's raw content (base64-decoded via `repos.getContent`), skips binaries via the same null-byte heuristic every other adapter uses, and populates `SyncResult.documents` so `ConnectorsService.sync()` actually persists them — previously it only ever returned `documentsSynced: 0`. `listFiles`/`downloadFile`/`getFileMetadata`/`searchFiles` were also rewritten to be real (`downloadFile` previously always returned an empty buffer; `getFileMetadata` called an API with hardcoded-empty `owner`/`repo` so it always failed silently). While adding a mocked-Octokit test for this, discovered `@octokit/rest` was never in `package.json` — `GitHubAdapter.authenticate()` would throw `Cannot find module '@octokit/rest'` on first real use in any actual deployment, meaning the GitHub connector didn't just have a fake `syncAll`, it couldn't even authenticate. Checked the other lazy-`require()`'d adapter dependency and found the same problem: `googleapis` was also missing from `package.json` despite `GoogleDriveAdapter` requiring it. Installed both as real dependencies. While fixing Google Drive's dependency, also audited its `syncAll` and found the same *shape* of bug as GitHub's, just less obvious: it looked functional (called `downloadFile`/`getFileMetadata` per file, incremented `documentsSynced`) but never populated `SyncResult.documents`, so `ConnectorsService.sync()` took the branch that just counts a number — zero `Document`/`Chunk` rows were ever created, meaning a "successful" Google Drive sync added nothing to the knowledge base. Fixed to build real `ConnectorDocument[]` entries, including exporting Google-native Docs/Sheets/Slides to text/csv via `drive.files.export` (they have no raw bytes to download directly) and skipping folders, binaries, and non-exportable native types (Forms, Drawings, etc.) with an explicit `skipped` counter instead of silently miscounting them as successes. 16 new unit tests (9 GitHub, 7 Google Drive — neither adapter had any test coverage before); 204 unit tests green, clean typecheck/build/lint. README/ROADMAP updated: GitHub moved from "auth-only" back to fully shipped, Google Drive's description now notes native-format export. **Not yet done**: neither fix has been exercised against a real GitHub repo or Google Drive account — verified via mocked Octokit/googleapis only.
18. ✅ e2e suite split by module (2026-09-18) — done: `test/api.integration.e2e-spec.ts` had grown to 1935 lines / 22 `describe` blocks / 78 tests in one file, all sharing one `beforeAll`-booted `INestApplication` and one set of Prisma/Neo4j/Qdrant/embedding/MinIO mocks — a maintainability problem (any merge conflict here touched unrelated modules) rather than a correctness one, since each test already set up its own mock return values with no cross-`describe` state coupling. Extracted the shared scaffolding into `test/support/e2e-app.ts` (mock factories + `bootstrapE2eApp()` which compiles `AppModule` with the same provider overrides and issues the same three role tokens + `resetE2eMockDefaults()` for the shared `beforeEach` reset logic) and `test/support/global-mocks.ts` (the five `jest.mock()` calls for uuid/amqplib/amqp-connection-manager/cache-manager-redis-yet/bcrypt, now wired in once via `jest-e2e.json`'s new `setupFiles` instead of being copy-pasted into every spec file). Split into 14 files under `test/api/` grouped by backend module (health, auth, keycloak-sso, documents, search-graph, chat, connectors, meetings, policies, notifications, invitations, expertise-gaps-recommendations, admin-users, viewer-role) — each boots its own isolated `INestApplication` instance, so no file can leak mock state into another. Verified 1:1 parity before deleting the original (`grep -c '  it('`: 78 in the old file, 78 across the 14 new ones) and confirmed identical pass/fail behavior after (same 78 passing; `app.e2e-spec.ts`'s pre-existing 2 DB-connectivity failures are unrelated and untouched). Trade-off accepted deliberately: e2e wall-clock went from ~25s (1 shared app boot) to ~88s (14 separate boots) for proper test isolation. **Not yet done**: e2e still isn't run in CI at all (`ci.yml`'s backend job only runs the unit-config `npx jest --passWithNoTests`) — that's a separate, pre-existing gap this pass didn't touch.
19. ✅ OneDrive + SharePoint connector adapters (2026-09-18) — done: OneDrive and SharePoint document libraries are both exposed by Microsoft Graph through the identical `/drive` resource shape (`/drives/{id}` vs `/sites/{id}/drive`), so built one shared `MicrosoftGraphDriveAdapter` abstract base (app-only client-credentials OAuth against `login.microsoftonline.com/{tenant}/oauth2/v2.0/token`, cached with expiry + 60s skew so repeated calls in one sync don't re-authenticate every request, `@odata.nextLink`-based pagination, folder filtering, binary-skip on download) plus two ~20-line subclasses (`OneDriveAdapter` resolving its drive via `driveId` or `userId`, `SharePointAdapter` via `siteId`). Both registered in `ConnectorRegistryService`; connector count now 10. Also fixed two stale credential hints in the Flutter connectors dropdown found while adding the new ones: GitHub's hint didn't mention `defaultRepo`, which the adapter has required since milestone 17's rewrite (following it exactly would hit "GitHub repo is missing"), and Google Drive's didn't mention `clientId`/`clientSecret`. 11 new unit tests (8 OneDrive covering token caching/pagination/sync, 3 SharePoint smoke tests for the site-scoped base path since the shared logic is already covered by OneDrive's suite); registry test extended to 10 types; 217 unit tests green, clean typecheck/build/lint, e2e still 78/80 (same 2 pre-existing unrelated failures). README/ROADMAP updated: OneDrive/SharePoint moved from Planned to Shipped, leaving Gmail/Outlook/Teams/Dropbox. **Not yet done**: not exercised against a real Azure AD app registration or tenant — verified via mocked fetch only, same caveat as every connector built this session.
20. ✅ Outlook connector adapter + Microsoft Graph auth extracted for reuse (2026-09-18) — done: pulled the client-credentials token logic out of `MicrosoftGraphDriveAdapter` into a standalone `MicrosoftGraphAuth` class (composed, not inherited, so a mail-resource adapter doesn't need to pretend to be a drive adapter) and refactored `MicrosoftGraphDriveAdapter` to use it — behavior-preserving, confirmed by re-running the existing OneDrive/SharePoint suites unchanged (still 11/11 green) before adding anything new. Built `OutlookAdapter` on top of the shared auth: lists/searches/downloads/syncs mail via `/users/{userId}/messages` (app-only auth has no signed-in "me", so `userId` is a required credential, same pattern as OneDrive's `driveId`/`userId`), `@odata.nextLink` pagination, `$search` with the required `ConsistencyLevel: eventual` header. Requests plain-text bodies directly via the `Prefer: outlook.body-content-type="text"` header instead of writing an HTML-to-text converter (Confluence needed one because its storage format doesn't offer a plain-text alternative; Graph mail does). Registered in `ConnectorRegistryService`; connector count now 11. 13 new unit tests (7 for the extracted `MicrosoftGraphAuth`, 6 for `OutlookAdapter`); 231 unit tests green, clean typecheck/build/lint, e2e still 78/80 (same 2 pre-existing unrelated failures). README/ROADMAP updated: Outlook moved from Planned to Shipped, leaving Gmail/Teams/Dropbox. **Not yet done**: not exercised against a real Azure AD app registration or mailbox — verified via mocked fetch only, same caveat as every connector built this session.
21. ✅ Gmail + Teams connector adapters (2026-09-18) — done: `GmailAdapter` follows `GoogleDriveAdapter`'s exact delegated-OAuth2 pattern (same `clientId`/`clientSecret`/`refreshToken` shape) rather than app-only auth, since that's what Google's consumer/Workspace OAuth actually offers for a single mailbox; the only new complexity is MIME: `messages.get` returns a `payload` tree of `parts` rather than a flat body, so a small recursive `extractPlainText` walks it looking for the first `text/plain` part and base64url-decodes it (Gmail uses URL-safe base64, not standard — `+`/`/` swapped for `-`/`_`, which the decode helper accounts for). `TeamsAdapter` reuses `MicrosoftGraphAuth` (the same class `OutlookAdapter` uses) against `/teams/{teamId}/channels/{channelId}/messages` — app-only auth needs both IDs explicitly, same pattern as every other Graph adapter's required target. Teams' `chatMessage` resource has no `Prefer: text` equivalent (that's Outlook-only), so it needed its own small HTML-to-text stripper, and Graph has no query/search endpoint for channel messages at all, so `searchFiles` filters client-side over the listed page (same fallback shape as `GitLabAdapter`'s search, which hit the equivalent gap for GitLab's basic-search tier). Both registered in `ConnectorRegistryService`; connector count now 13 — every connector in the original README's "Planned" list is shipped except Dropbox. 13 new unit tests (6 Gmail, 7 Teams); 246 unit tests green, clean typecheck/build/lint, e2e still 78/80 (same 2 pre-existing unrelated failures). README/ROADMAP updated: Gmail and Teams moved from Planned to Shipped, leaving only Dropbox. **Not yet done**: not exercised against a real Gmail account or Teams tenant — verified via mocked googleapis/fetch only, same caveat as every connector built this session.
22. ✅ Dropbox connector adapter (2026-09-18) — done: the last connector from the original README's "Planned" list. `DropboxAdapter` supports two auth shapes rather than one, since Dropbox app setups genuinely vary here: a long-lived `accessToken` used as-is (no expiry tracking — externally managed), or `refreshToken` + `clientId` + `clientSecret` exchanged at `api.dropboxapi.com/oauth2/token` (`grant_type=refresh_token`) and cached with the same expiry-skew pattern as `MicrosoftGraphAuth`, since Dropbox's short-lived tokens genuinely expire in ~4h. Dropbox splits its API across two hosts — `api.dropboxapi.com` for metadata (`list_folder`/`list_folder/continue` for recursive cursor-paginated listing, `search_v2`, `get_metadata`) and `content.dropboxapi.com` for the actual `files/download`, which takes its path argument in a `Dropbox-API-Arg` JSON header rather than the request body like every other call — the one real gotcha in an otherwise standard REST adapter. Registered in `ConnectorRegistryService`; connector count now 14. Caught by lint, not by a human: a test helper used `fetchSpy.mock.calls.at(-1)?.[1].headers` to recover the download call's arguments, which `no-unsafe-optional-chaining` correctly flagged as capable of throwing if the call array were ever empty — fixed by reading the arguments Jest already hands the mock callback instead of reaching back into its call history. 10 new unit tests covering both auth shapes (long-lived token: auth/list/paginate/download/search/sync; refresh-token: exchange/missing-credentials/caching); 257 unit tests green, clean typecheck/build/lint, e2e still 78/80 (same 2 pre-existing unrelated failures). README/ROADMAP updated: Dropbox moved from Planned to Shipped — every connector named in the original README is now real. **Not yet done**: not exercised against a real Dropbox app/account — verified via mocked fetch only, same caveat as every connector built this session; and more broadly, none of the 14 connectors built across this work has been run against live credentials, which is the natural next validation step once real accounts are available.
23. ✅ e2e wired into CI + a workflow bug found along the way (2026-09-18) — done: `ci.yml`'s backend job ran unit tests only (`npx jest --passWithNoTests`, which only picks up `src/**/*.spec.ts` under the unit `jest` config in `package.json` — `test/api/*.e2e-spec.ts` was never in scope). Added an `npm run test:e2e` step using the CI job's existing Postgres service (`DATABASE_URL` pointed at it, same connection string the `prisma generate` step already uses) — no new service container needed, since only `app.e2e-spec.ts` touches a real Prisma connection (a plain `$queryRaw` ping via `PrismaHealthIndicator`, which needs a live Postgres but no deployed schema/migrations) and the 14 split files all run against a fully mocked `PrismaService`. This should also fix the 2 e2e failures that have shown up in every local run this session (`app.e2e-spec.ts` needs `DATABASE_URL`, which this sandbox never had) — CI's Postgres service should make all 80 e2e tests pass there, not just 78. While reading the file to place the new step, found `zap-baseline:` sitting at column 0 — a sibling of `jobs:` rather than nested inside it, despite having a `runs-on`/`steps` shape identical to the other three jobs. An extra top-level key like this is outside the GitHub Actions workflow schema, which typically invalidates the entire workflow file rather than just the one job — meaning this could have been silently breaking every job in the file, not only ZAP. Fixed by indenting the single `zap-baseline:` line to 2 spaces; its child fields were already sitting at the correct 4-space depth (matching the `docker:` job's pattern exactly), so no other lines needed to move. Verified with `yaml.safe_load` that `zap-baseline` now appears under `jobs` and that the new e2e step landed in the right position in `backend`'s step list; also re-ran the e2e suite locally with `NODE_ENV=test` set (matching what CI now sets) to confirm no env-dependent behavior changes — same 78/80 result. **Not yet done**: this has not been verified by an actual GitHub Actions run (no push access from this environment) — the fix is verified by YAML parsing and local-equivalent test runs, not by watching the workflow execute for real. Whether the `zap-baseline` bug was actually causing the whole file to be rejected (as opposed to GitHub silently ignoring just the one bad key) is inferred from how the Actions schema is documented to behave, not confirmed against a live run.
24. ✅ First real CI run confirms the theory, surfaces two more pre-existing bugs (2026-09-18) — pushed milestones 1–23 to `origin/main` and watched the run. Confirmed: `main`'s prior 4 CI runs had all failed in `0s` — consistent with the whole workflow file being rejected as invalid before the `zap-baseline` indentation fix, exactly as predicted. This run got further: `zap-baseline` and `docker` both now appear as real, correctly-gated jobs (the former correctly skipped on a push trigger, per its own `if:` condition) — confirming that fix actually works, not just parses. But it also surfaced two bugs neither previous work nor local runs could have caught, since neither job had ever successfully executed before: (1) the `postgres` service's `options: --health-cmd pg_isready -U postgres ...` has an unquoted multi-word health-cmd — GitHub Actions splits `options` on whitespace, so `-U postgres` was passed straight to `docker create` as if they were its own flags (`unknown shorthand flag: 'U'`, exit 125, before any of the new e2e step or any other backend step could run). Fixed by quoting the health-cmd value: `--health-cmd "pg_isready -U postgres"`. (2) The frontend job's `flutter analyze` failed on `pubspec.yaml` declaring two asset directories, `assets/images/` and `assets/icons/`, that don't exist anywhere in the repo and are referenced by zero Dart files (`grep -rn "assets/images/\|assets/icons/" frontend/lib` — no hits) — vestigial scaffold config that `flutter analyze` treats as a warning, and warnings fail the exit code. Removed the dead `assets:` block from `pubspec.yaml` rather than inventing placeholder asset files for paths nothing uses. Verified locally, not just by inference this time: `flutter pub get && flutter analyze` → "No issues found!", `flutter build web --release --no-tree-shake-icons` → "✓ Built build\web". **Not yet done**: haven't re-pushed and watched a second run yet to confirm both fixes together get the backend and frontend jobs to a real green — that's the natural next step.
25. ✅ Second CI run: both prior fixes confirmed live, one more bug found (2026-09-18) — pushed and watched again. `flutter analyze` passed in real CI this time ("No issues found!" equivalent — the job got past it clean), and the backend job's `Initialize containers` step succeeded too (Postgres actually started and logged its normal boot sequence) — both confirming the fixes from entry 24 work for real, not just locally. New failure, further downstream than either previous one: `npm ci` failed with "Missing: @emnapi/core@1.11.3 from lock file" — `package-lock.json` had drifted out of the exact sync `npm ci` requires (unlike `npm install`, which just re-resolves), almost certainly from months of `npm install <pkg>` calls across this whole connector-building effort updating the lock incrementally rather than everything being regenerated together. `npm ci` locally on this Windows sandbox worked fine against the old lockfile — the missing package was Linux-platform-specific (nested under `@unrs/resolver-binding-wasm32-wasi`, a WASM fallback dependency whose own resolved version in the lock didn't match what its declared `optionalDependencies` needed), so it could never have been caught without a Linux run, which is exactly how it surfaced. Fixed by deleting `package-lock.json` and `node_modules` and running a fresh `npm install` to force full re-resolution rather than patching the drift by hand. That surfaced a second issue on its own: `@qdrant/js-client-rest` floated from the previously-locked `1.18.0` to `1.19.0` under its `^1.18.0` range, and `1.19.0` removed/renamed `QdrantClient.search()` — a semver-violating breaking change in a "compatible" bump, caught immediately by `tsc` (`Property 'search' does not exist on type 'QdrantClient'`). Rather than migrate `QdrantService`'s real, working, already-tested `.search()` usage to whatever `1.19.0` renamed it to — an unplanned, unrelated scope expansion — pinned `@qdrant/js-client-rest` to the exact working version (`"1.18.0"`, no caret) in `package.json` and regenerated again. Verified thoroughly before pushing again, unlike the first two fixes: `npm ci` clean, `tsc --noEmit` clean, `npm run build` clean, full unit suite 257/257, e2e 78/80 (same 2 pre-existing unrelated failures), lint at the same 4 pre-existing errors. **Not yet done**: this fix itself hasn't been through a live CI run yet — everything above it now has, but this one is still only locally verified, same caveat every other fix in this file started with before its own follow-up entry.
26. ✅ Third CI run: lockfile + Qdrant pin confirmed live, audit gate hit the real pre-existing vulnerabilities (2026-09-18) — pushed and watched again. `npm ci` succeeded in real CI this time, confirming entry 25's lockfile regeneration actually fixed the Linux-specific drift (couldn't be verified locally on this Windows sandbox, only inferred). New failure, one step later: `npm audit --audit-level=high`, which — like every other step this deep in the job — had never actually executed in CI before, since the workflow was invalid until entry 24. See the "Known gaps" entry above for the three vulnerability chains and the decision to lower the gate to `--audit-level=critical` rather than force a NestJS major-version migration into this session. Confirmed locally that `critical` passes clean (exit 0, same 17 high findings surfaced in the report but not gating). **Not yet done**: same as every fix in this thread until proven otherwise — not confirmed by an actual CI run yet.
27. ✅ Fourth CI run: audit gate confirmed live, lint hit 4 more previously-hidden pre-existing errors (2026-09-18) — pushed and watched again. `npm ci`, `npm audit --audit-level=critical`, and `npx prisma generate` all passed for real this time, confirming entries 25 and 26 both work live. New failure: `npm run lint` — a step that, like every one before it in this thread, had never actually executed in CI before now. The 4 errors were not new: same `@typescript-eslint/require-await` findings ("Async method has no 'await' expression") tracked as pre-existing all session, in `keycloak.service.spec.ts` (untouched by any of this session's work) and `keycloak-sso.e2e-spec.ts` (created during the e2e-split, milestone 18, by copying the pattern verbatim from the original file). Unlike the audit findings, these needed no user decision — trivial, zero-behavior-change fixes: each was a Jest mock (`json: async () => ({...})`, `mockImplementation(async (args) => ({...}))`) declared `async` but returning a plain value with no `await` inside; since the surrounding types are all `Record<string, any>` mocks with no strict `Promise`-returning signature to satisfy, and every call site already does `await mockPrisma.user.create(...)` (which unwraps a non-Promise value identically to a Promise-wrapped one), removing the unnecessary `async` keyword changes nothing at runtime. Verified before pushing: `eslint` → 0 errors (1347 warnings, same pre-existing count), `tsc --noEmit` clean, the specific `keycloak.service.spec.ts` suite (9/9) and the full unit suite (257/257) both green, `npm run build` clean, e2e 78/80 (same 2 pre-existing unrelated failures). **Not yet done**: not confirmed by an actual CI run yet — if this one goes green, `Backend — Lint, Test & Build` should finally reach `npx jest --passWithNoTests` and the new `npm run test:e2e` step for the first time ever.
28. ✅ Fifth CI run: lint, unit tests, and e2e all pass live for the first time ever — two Docker build bugs found last (2026-09-18) — pushed and watched again. `npm run lint`, `npx jest --passWithNoTests`, `npm run test:e2e`, and `npm run build` **all passed in real CI**, meaning the e2e suite (milestones 18 + 25's whole point) has now genuinely run green against a live Postgres service, not just been inferred safe from local runs. Both jobs then failed at their Docker build step — the last step in each, so the deepest either job had ever gotten. Two unrelated, pre-existing bugs, neither touched by anything this session did before now: (1) `Dockerfile.backend`'s builder stage ran `npm ci --only=production` before `npm run build`, but `nest build` needs `@nestjs/cli` — a devDependency — so the build step failed with `sh: nest: not found`. Fixed to the standard multi-stage pattern: full `npm ci` (with devDependencies) to build, then `npm prune --omit=dev` afterward so the image copied into the `production` stage stays lean. Verified the prune step doesn't strip anything the app actually needs at runtime by actually starting the built app locally against the pruned `node_modules` — it got through full NestJS bootstrap (all 14 connectors registered, all infra modules initialized) before failing only on the expected missing live Postgres connection, exactly as it should with no other infra available. (2) `Dockerfile.frontend` referenced `FROM flutter:3.35`, but there is no official Docker Hub `library/flutter` image — Flutter has never published one, and `flutter` under the bare `library/` namespace doesn't exist at all, so every build of this Dockerfile must have always failed at the very first line, for as long as this line has existed. Fixed to `ghcr.io/cirruslabs/flutter:3.35.7` — the de facto standard community Flutter image, version-matched to what `ci.yml`'s own `flutter-version: "3.35.x"` already resolves to (confirmed via the GHCR registry API that the exact tag exists and is pullable before using it, not assumed). **Not yet done**: not confirmed by an actual CI run yet, and specifically the `docker build` steps themselves — verified the backend fix's *logic* locally (prune doesn't break the app) since no Docker daemon is available in this sandbox to run either build directly.
29. ✅ Sixth CI run: backend Docker build confirmed live, one more build-context bug found and fixed, seventh run reaches full green (2026-09-18) — pushed and watched again. The backend job passed end-to-end for the first time, confirming entry 28's `npm prune --omit=dev` fix works for real, not just locally. The frontend job's Docker build still failed, on a different line: `Dockerfile.frontend`'s `COPY nginx.conf /etc/nginx/conf.d/default.conf` errored with `"/nginx.conf": not found`. Root cause: both `ci.yml` (`working-directory: frontend`, `docker build -f ../docker/Dockerfile.frontend .`) and `docker/docker-compose.yml` (`context: ../frontend`) build the frontend image with `frontend/` as the build context, but `nginx.conf` physically lived in `docker/nginx.conf` — outside that context, and `COPY` cannot reach outside its build context. This would have broken `docker-compose up` identically, independent of the Flutter base-image fix in entry 28; it just hadn't been reached yet. Fixed by moving the file to `frontend/nginx.conf` (`git mv`, no Dockerfile changes needed since it already referenced the bare relative filename). Pushed and watched a seventh run: **Backend ✓, Frontend ✓ (including its Docker build), and the `Docker — Compose Validation` job (`docker compose -f docker/docker-compose.yml config`) all passed live** — the entire CI pipeline is now fully green end-to-end for the first time this engagement, covering lint, unit tests, e2e tests, `npm run build`/`flutter build web`, and both production Docker image builds. `zap-baseline` correctly no-shows on a push trigger (gated to `schedule`/`workflow_dispatch` only, since it needs a live `STAGING_URL`) — expected, not a failure. **Not yet done**: the ZAP baseline job itself still hasn't run against real infrastructure (no staging environment deployed from this sandbox), and none of the 14 connectors have been exercised against live credentials — both remain open, tracked below.
30. ✅ OpenSearch provisioned in CI, closing milestone 14's real-cluster gap — one more pre-existing bug found along the way (2026-09-18) — done: since milestone 14, `OpenSearchService` had unit-test coverage via mocks only; every e2e spec file also left it unmocked by omission, which meant it silently relied on there being no reachable cluster in this sandbox to exercise the Postgres ILIKE fallback path — an implicit, accidental determinism rather than a designed one. Fixed properly: `bootstrapE2eApp()` in `test/support/e2e-app.ts` now explicitly overrides `OpenSearchService` with a mock (`isAvailable()` defaulting to `false`) like every other infra service, plus a new `realOpenSearch: true` option that skips that override for the one suite that needs a live connection. Added `test/api/opensearch-integration.e2e-spec.ts`, gated behind `describe.skip` when `OPENSEARCH_HOST` isn't set (so it skips cleanly on every local run, same as this sandbox) and exercising the real cluster when it is: connects, indexes a chunk, searches it back with BM25 org-scoped filtering (and confirms a different org gets zero hits), deletes it, confirms the delete is immediately visible. That last assertion caught a real bug: `OpenSearchService.deleteByDocumentId` called `deleteByQuery` without `refresh: true` (unlike `indexChunks`, which already had it), so a search run immediately after a delete could still return the stale document until OpenSearch's next ~1s refresh cycle — fixed by adding `refresh: true` there too, matching the existing index-side pattern. `ci.yml`'s backend job now runs an `opensearch` service container (same image/version as `docker/docker-compose.yml`, health-checked before the e2e step runs) so this test actually runs in CI, not just locally with a cluster nobody has. While configuring the container's `OPENSEARCH_INITIAL_ADMIN_PASSWORD`, found that `docker-compose.yml` (and the matching `.env.example`) had it set to `admin` — OpenSearch 2.12+ enforces a strong-password check on that variable (min 8 chars, upper/lower/digit/special) and refuses to start otherwise, so the compose file's OpenSearch service could never have come up on the pinned `2.16.0` image; fixed both to a compliant shared value and updated the healthcheck/backend-env lines that referenced the old password to match. Verified locally: `tsc --noEmit` clean, full unit suite 257/257 (the `opensearch.service.spec` assertion on `deleteByDocumentId` uses `objectContaining`, so it wasn't broken by the added `refresh: true` field), e2e 78/80 (same 2 pre-existing unrelated failures) with the new integration spec correctly skipping since this sandbox has no reachable `OPENSEARCH_HOST`. **Confirmed live**: pushed and watched — the real cluster started, passed its health check, `OpenSearchService` connected for real ("Connected to OpenSearch at https://localhost:9200"), and `PASS test/api/opensearch-integration.e2e-spec.ts` ran (not skipped) against it, while the unit-test run's mocked `OpenSearchService` stayed correctly isolated and reported unavailable — both paths verified simultaneously in one CI run.
31. ✅ Prompt-injection context sanitization — implemented the follow-up SECURITY_SPEC.md section 7 had flagged as not done (2026-09-18) — done: `SECURITY_SPEC.md` promised "instruction-filter classifier, context sanitization" as a follow-up with a `(ROADMAP)` pointer that didn't actually exist anywhere in this file — a dangling reference, alongside a second stale claim in the OWASP checklist ("`npm audit --audit-level=high` gate... 0 high+") that milestone 26 had already made false by lowering the gate to `critical` over 17 known high-severity findings; both fixed as part of this entry. The real gap: retrieved chunks (vector/graph/keyword search results) were placed into the LLM prompt as "Context" with no sanitization — a RAG system is exploitable if an attacker gets injected text into any indexed source (a malicious PDF upload, a synced Slack message, a wiki page) that then gets retrieved and treated as instructions rather than data. Worse, this logic existed in two independent copies — `ChatService.formatContext` (REST) and `ChatGateway.streamResponse` (WebSocket streaming) had each hand-rolled their own `[Source #n]`-style formatting, so a defense added to only one would have left the other exposed. Extracted a shared `infrastructure/ai/prompt-sanitizer.ts` (`sanitizeChunkContent` + `formatRetrievedContext`) used by both: neutralizes instruction-override attempts ("ignore/disregard/forget ... previous instructions"), system-prompt exfiltration requests ("reveal your system prompt"), jailbreak framing ("you are now in developer mode"), and spoofed chat-role headers (`system:`/`assistant:`/`developer:` at line start) — targeted, low-false-positive heuristics rather than an exhaustive classifier, explicitly scoped that way in the spec update rather than overclaiming. The whole context block is also wrapped in explicit untrusted-data framing as a second, pattern-independent layer. 12 new unit tests for the sanitizer itself (including a benign-content case confirming no false positives on ordinary text that happens to contain words like "system" or "ignore" in harmless contexts), plus one regression test each in `chat.service.spec.ts` and `chat.gateway.spec.ts` proving the sanitization actually reaches the real `openai.chat.completions.create` call on both paths, not just the isolated helper — the gateway test needed a new `jest.mock('openai', ...)` since `ChatGateway.streamResponse` had never had its LLM call path tested at all before. Caught one test-isolation smell while writing the `ChatService` regression test: `jest.clearAllMocks()` clears call history but not `mockReturnValue`/`mockResolvedValue` implementations, so `mockOpenSearch.isAvailable` was silently carrying `true` over from an earlier test in the same file — fixed by setting it explicitly rather than relying on describe-block ordering. 271 unit tests green (up from 257), `tsc --noEmit` and `npm run build` clean, lint 0 errors. **Confirmed live**: pushed and watched — Backend, Frontend, and Docker Compose Validation all passed, with the new sanitizer/regression tests running as part of the green unit suite.
32. ✅ Frontend API base URL was never actually configurable — fixed the real bug, not just the symptom (2026-09-18) — done: `FRONTEND_SPEC.md`'s "Known gaps" already named this ("`ApiClient` base URL hardcoded to localhost — needs build-time config"). Investigating it surfaced something worse than an unfixed gap: someone *had* already tried to fix it, in both `docker-compose.yml` and `k8s/frontend-deployment.yml`, by setting `API_BASE_URL` as a container `environment:` variable — which does nothing, because a Flutter web build is static once compiled (`flutter build web` runs once, at image-build time) and nginx serving the resulting files at container startup has no code path that ever reads that variable. In production this meant the frontend image would always call whatever URL was hardcoded into `ApiClient` at the point the image was built — `http://localhost:3000/api/v1`, meaningless from a real user's browser — regardless of what the k8s manifest claimed to set, silently and with no error. Fixed at the actual layer that matters: `ApiClient.defaultBaseUrl` now resolves via `String.fromEnvironment('API_BASE_URL', defaultValue: 'http://localhost:3000/api/v1')`, a compile-time constant; `docker/Dockerfile.frontend` gained `ARG API_BASE_URL` threaded into `flutter build web --dart-define=API_BASE_URL=${API_BASE_URL}`; `docker-compose.yml`'s frontend service moved `API_BASE_URL` from the dead `environment:` block to `build.args` (which actually reaches the Dockerfile ARG); `k8s/frontend-deployment.yml`'s dead `env:` block was replaced with a comment pointing at the real mechanism (`docker build --build-arg`, at image-build time, not the manifest). Verified end-to-end with the real local Flutter SDK, not just by inspection: built the web bundle with `--dart-define=API_BASE_URL=https://api.example-staging.com/api/v1` and grepped the compiled `main.dart.js` — the custom URL was present and the `localhost:3000` default was completely gone; rebuilt with no override and confirmed the default came back, so zero-config local dev is unaffected. `flutter analyze` clean, full frontend suite 28/28 green. **Not yet done**: not yet confirmed by a live CI run (frontend job doesn't pass a custom `--build-arg`, by design, since it's just validating the default build still works — a real deployment pipeline would need to supply the real backend URL when building the production image, which is outside what this repo's CI does today).
33. ✅ Responsive shell — NavigationRail was always-on regardless of viewport width (2026-09-18) — done: `FRONTEND_SPEC.md` already named this as planned ("expands to BottomNavigation for mobile"), and it was a real, currently-broken layout, not just an unfinished nicety — `AppShell`'s `NavigationRail` used `labelType: NavigationRailLabelType.all` unconditionally, so on an actual phone-width viewport its 8 labeled destinations would consume most of the available width, leaving almost no room for the routed content. Rewrote `AppShell` to check `MediaQuery.sizeOf(context).width` against Material's own compact/medium breakpoint (600px): below it, a bottom `NavigationBar` (`onlyShowSelected` label behavior to fit 8 destinations); at or above it, the existing `NavigationRail`. Extracted the 8 destinations (icon/selectedIcon/label/route) into one shared list so both layouts — and the notification-count badge logic — are built from a single source instead of duplicating the definitions, and simplified `_currentIndex`'s per-route `if` chain into a loop over that same list. Added a new widget test that sets a 375×800 test viewport (`tester.view.physicalSize`) and asserts the bottom `NavigationBar` renders and `NavigationRail` doesn't — the inverse case of the existing test, which already asserted `NavigationRail` at the default 800×600 test surface and continues to pass unchanged, confirming the breakpoint didn't disturb the wide-viewport path anything depended on. `flutter analyze` clean, full suite 29/29 green (up from 28).
34. ✅ API_SPEC.md drift audit — two real gaps found and fixed (2026-09-18) — done: after finding stale/inaccurate documentation twice already this session (SECURITY_SPEC.md's dangling reference and stale audit-gate claim; FRONTEND_SPEC.md's documented gap hiding a deeper dead-config bug), audited `API_SPEC.md` systematically against every real controller's routes/guards/roles rather than assuming it was still accurate. Two real discrepancies, both the kind that would mislead someone integrating against the API: (1) the connector adapters registry line said `GOOGLE_DRIVE, SLACK, GITHUB` — 3 types — while the actual registry has shipped all 14 since milestone 22 of the earlier connector-building work; a reader would believe Notion/Jira/Linear/Confluence/GitLab/OneDrive/SharePoint/Outlook/Gmail/Teams/Dropbox don't exist. (2) `POST /auth/sso/keycloak` and `GET /auth/sso/keycloak/status` — both real, tested endpoints resolved back on 2026-08-20 — were entirely absent from the Authentication table. Everything else in the doc (all 16 controllers' methods/paths/role requirements, WebSocket event names, the `/metrics` path resolution through the global prefix) was verified accurate — this wasn't a wholesale rewrite, just two specific fixes. Both corrected in place.
35. ✅ Meeting summarization was a hardcoded stub, not an LLM call — found chasing the same stale-3-connector-list bug into two more docs, fixed the real code (2026-09-18) — done: grepping the repo for the same stale connector-list phrasing that milestone 34 fixed in `API_SPEC.md` turned up two more copies — `docs/specifications/PROJECT_SPEC.md` and `README.md`'s intro bullet (the dedicated "Universal Connectors" section elsewhere in the same README was already accurate; only this one summary line had been missed) — both corrected. That same pass through `PROJECT_SPEC.md` surfaced something much bigger: its "Non-goals" section explicitly claimed Keycloak SSO was only "schema-ready" and OCR's "engine not integrated" — both false, both fully shipped weeks ago — and its "Future roadmap" line listed five already-done features (user management, OCR, SSO/Keycloak, admin dashboards UI, and "meeting transcript AI") as if none of them existed. Chasing down whether "meeting transcript AI" was really done led straight to `MeetingsService.generateSummary` — and it wasn't: `const summary = \`AI-generated summary for "${meeting.title}"\`;` and `const actionItems = ['Action item 1', 'Action item 2'];` — a hardcoded template string and literal placeholder strings, never once reading `meeting.transcript`, with zero unit or e2e test coverage that could have caught it. Exactly the same shape of bug as milestone 17's GitHub/Google-Drive `syncAll()` stubs: a real endpoint, a real DB write, an API response that looks entirely plausible — just no actual work happening behind it. Rewrote it for real: calls the OpenAI client (same lazy-getter pattern as `ChatService`) with `response_format: json_object`, asking for `{summary, actionItems, decisions}` from the actual transcript; the transcript is run through `sanitizeChunkContent` (milestone 31's prompt-injection sanitizer) first, since a meeting transcript pasted or synced from a third-party service is exactly the kind of untrusted content that helper was built for; a missing transcript now throws `400 Bad Request` instead of silently fabricating a summary for nothing; an LLM failure or malformed JSON response now falls back to an honest "AI summary generation is currently unavailable" message with empty arrays rather than fake placeholder content. 7 new unit tests (`meetings.service.spec.ts` — the module had none before) covering the JSON-parse happy path, both failure fallbacks, the 404/400 validation, and a regression test proving the sanitizer reaches the real LLM call, plus 3 new e2e cases (missing-transcript 400, cross-org 404, and the graceful-degradation path that's all this no-API-key CI/local environment can ever exercise — matching how `chat.e2e-spec.ts` already tests `ChatService`'s equivalent fallback). Also fixed README's "Meeting Intelligence" feature bullet, which claimed "Transcription... entity extraction" — neither is true; meetings never touch the document/graph pipeline, and audio-to-transcript recording doesn't exist, only summarization of an already-provided transcript. 278 unit tests green (up from 271), e2e 81 passing / 85 total (up from 78/80 — same 2 pre-existing unrelated failures, plus 2 correctly-skipped OpenSearch-integration cases), `tsc --noEmit`/`npm run build` clean, lint 0 errors. **Not yet done**: not yet confirmed by a live CI run; real audio-to-transcript capture and automated policy-compliance checks (as opposed to policy search, which is real) remain genuinely future work, now accurately reflected in `PROJECT_SPEC.md`.
